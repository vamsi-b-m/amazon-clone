# ------ Global -----------------------------------------------------------------
global:
  namespace: amazon-clone

# ------ Image ----------------------------------------------------------------
image:
  repository: vamsibm36
  tag: 1.0.0
  pullPolicy: Never             # Never for kind (local), always for EKS

# ------ Order Service ----------------------------------------------------------------
orderService:
  replicas: 1
  port: 3004
  resources:
    requests:
      memory: "128Mi"
      cpu: "100m"
    limits:
      memory: "256Mi"
      cpu: "500m"
