# helm/amazon-clone/templates/user-service.service.yaml
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace:  {{ .Values.global.namespace }}
  labels:
    app: user-service
spec:
  selector:
    app: user-service
  ports:
    - name: http
      port: {{ .Values.userService.port }}
      targetPort: {{ .Values.userService.port }}
