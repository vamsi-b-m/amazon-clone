# k8s/user-service/service
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: amazon-clone
spec:
  selector:
    app: user-service
  ports:
    - port: 3001
      targetPort: 3001
