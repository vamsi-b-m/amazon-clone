# k8s/user-service/service
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: amazon-clone
spec:
  selector:
    app: notification-service
  ports:
    - port: 3005
      targetPort: 3005
