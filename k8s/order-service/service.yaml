# k8s/user-service/service
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: amazon-clone
spec:
  selector:
    app: order-service
  ports:
    - port: 3004
      targetPort: 3004
