# k8s/user-service/service
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: amazon-clone
spec:
  selector:
    app: api-gateway
  ports:
    - port: 4000
      targetPort: 4000
